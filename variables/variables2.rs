fn main() {
    // Variables declared with `let` must be given a value before they can be read
    let x = 10;
    if x == 10 {
        println!("x is ten!");
    } else {
        println!("x is not ten!");
    }
}
