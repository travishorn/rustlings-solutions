fn main() {
    // Booleans (`bool`)
    // Can be true or false
    // Useful in conditional logic like `if` statements

    let is_morning = true;
    if is_morning {
        println!("Good morning!");
    }

    let is_evening = false;
    if is_evening {
        println!("Good evening!");
    }
}
